export enum WalletType {
  unknown = 'unknown',
  discover = 'discover',
  elf = 'elf',
  portkey = 'portkey',
}

export enum WebLoginState {
  initial = 'initial',
  lock = 'lock',
  eagerly = 'eagerly',
  logining = 'logining',
  logined = 'logined',
  logouting = 'logouting',
}
